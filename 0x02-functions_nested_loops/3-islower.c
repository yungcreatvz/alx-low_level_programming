#include <stdio.h>
#include "main.h"

/**
 * _islower - Print check lower char function
 * followed by a new line
 * Return: Always 0 (Success)
 */
int _islower(int c)
{
	return(c >= 'a' && c <= 'z');
}
