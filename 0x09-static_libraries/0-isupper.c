#include <stdio.h>
#include "main.h"

/**
 * _isupper - Print check uppercase
 * @c: character input
 * followed by a new line
 * Return: 0 (Success) 1 (error)
 */
int _isupper(int c)
{
	return (c >= 'A' && c <= 'Z');
}
