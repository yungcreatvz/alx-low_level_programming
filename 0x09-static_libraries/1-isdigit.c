#include <stdio.h>
#include "main.h"

/**
 * _isdigit - Print check digits
 * @c: character input
 * followed by a new line
 * Return: 0 (Success) 1 (error)
 */
int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
