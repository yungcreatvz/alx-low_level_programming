#include <stdio.h>
#include "main.h"

/**
 * _islower - Print check lowercase
 * @c: character input
 * followed by a new line
 * Return: 0 (Success) 1 (error)
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
