#include <stdio.h>
#include "main.h"

/**
 * _abs  - return absolute integer
 * @a: character input
 * followed by a new line
 * Return: absolute int
 */
int _abs(int a)
{
	if (a > 0)
		return (a);
	return (-a);
}
